#!/usr/bin/env python3
import sys


# sys.stdout.buffer.write(b'jiajingw\0AA+')
# original_string = 'jiajingw'
# padded_string = original_string.ljust(9)
# byte_sequence = padded_string.encode() + b'\0' + b'A+'
sys.stdout.buffer.write(b'jiajingw\0AA+')


# sys.stdout.buffer.write(byte_sequence)
